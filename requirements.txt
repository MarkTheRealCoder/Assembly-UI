# Core GUI (Qt widgets, SVG, events)
PyQt5>=5.15.0

# Code editor (PyQt5.Qsci)
QScintilla>=2.13.0

# HTML help viewer (QtWebEngineWidgets, QtWebChannel)
PyQtWebEngine>=5.15.0

# Toast notifications (import pyqttoast)
pyqt-toast-notification>=1.3.0

# Java interpreter bridge (import jpype)
jpype1>=1.4.0

# Disk enumeration, regex in file dialogs / QSS
psutil>=5.9.0
regex>=2023.0.0

# Windows-only: native window chrome, shell APIs (win32api, win32gui, QtWinExtras)
pywin32>=306; sys_platform == "win32"
